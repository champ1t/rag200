# Miniminal Gitignore to allow Full Backup
# Only ignore virtual environments and secrets
venv/
.venv/
venv-py311/
env/
ENV/
.env
.env.*
*.secret
config/secrets.yaml

# Include EVERYTHING else as requested
# (data, backups, tests, src, etc.)
